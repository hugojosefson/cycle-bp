{
  "name": "cycle-bp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "mkdir -p dist && beefy src/index.js:dist/index.js -- -t babelify",
    "build": "mkdir -p dist && browserify src/index.js -t babelify > dist/index.js "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cycle/core": "^5.0.1",
    "@cycle/dom": "^7.1.1",
    "rx": "^4.0.7"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.1.18",
    "babelify": "^7.2.0",
    "beefy": "^2.1.5",
    "browserify": "^12.0.1"
  }
}
